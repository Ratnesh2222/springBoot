Readme for springBoot

To run jav in detached mode: & make it detached
java -jar ./target/myspringboot-0.0.1-SNAPSHOT.jar &

How to kill running background process
ps -aux (show the list of process)
kill pid (to kill process id)

Set eclipse setting in itellij
setting-> keymap-> select eclipse from drop down

Problem : If a interface class implimented by multiple class then issue come during compile time that which one will be use
Solution : @Primary : add on the class which implimentation you what
           @Qualifier : added on class wher injection happen and keep first character small

